{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen 9003 launch",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/code/": "${workspaceFolder}"
            }
        }
    ]
}